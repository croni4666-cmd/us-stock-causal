# us-stock-causal dependencies
# requirements.txt — minimum versions (floating)
# requirements-lock.txt — exact pinned versions (reproducibility)
# bump after testing, 跟 paper-agent 模式一致

# Core data platform (~300 MB with [ta] extra)
openbb[ta]>=4.4.0

# Plotting
matplotlib>=3.11.1
mplfinance>=0.12.10b0
plotly>=5.18.0

# Data
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0

# Technical analysis (kept from v0.2 for reference, OpenBB also provides)
pandas-ta>=0.4.0

# Causal analysis (Pearl-style, Phase 9)
causal-learn>=0.1.4.8
dowhy>=0.14

# Stats / graph
statsmodels>=0.14.0
networkx>=3.0
pydot>=4.0.0

# SEC EDGAR (mavis sec-filings-fetch skill, R2 license)
edgar-parser>=0.3.1
rapidfuzz>=3.14.6

# HTTP / retry
requests>=2.33.0
tenacity>=9.0.0
yfinance>=0.2.40

# Utilities
pyyaml>=6.0
loguru>=0.7.0
python-dotenv>=1.2.3

# Dev / test (optional)
pytest>=7.0.0
