v0.6.8m: P5-3 + P5-4 done — Windows Task Scheduler 17:00 daily

按 user 2026-07-27 决定: 17:00 (美股收盘后 1h) 注册 Windows Task Scheduler,
整个 Phase 5 (本地化) 路径终于跑通: task 自动调 daily_report.py 7 步 pipeline,
写本地 log, 失败 exit code 给 P8 异常检测用。

== 新增 ==

- scripts/run_daily_report.cmd (~1.5KB, ASCII-only)
  - 解析 PROJECT_ROOT: strip trailing `\` from %~dp0 + \.. + absolute via `for %%I`
  - 用 PowerShell `(Get-Date -Format 'yyyy-MM-dd')` 拿稳定日期 (替代 wmic + substr)
  - output\logs\cron_<date>.log 写当日 stdout+stderr (覆盖旧 log)
  - exit code passthrough (P8 alert check 用)

- scripts/install_task.cmd (~1.9KB, ASCII-only)
  - schtasks /Create /SC DAILY /TN "us-stock-causal-daily-report" /TR "\"<cmd_path>\"" /ST 17:00 /F
  - **幂等**: /F 覆盖, 重复跑不报错
  - 失败提示: admin / 时间格式 / 路径特殊字符
  - 默认 17:00, 可 `install_task.cmd 16:30` 覆盖

- scripts/setup_windows_task.ps1 (~4.1KB, ASCII-only, 备选路径)
  - PowerShell `Register-ScheduledTask` 完整配置
  - `-Time` 参数, `-Uninstall` 开关
  - `Start-WhenAvailable` (笔记本 sleep 后补跑) + `RestartCount 3` (yfinance 重试)
  - 注: 实测发现 PS 5.1 `Register-ScheduledTask` 在 non-elevated session 报
    "Access is denied", .cmd 走 schtasks 在 admin cmd 下稳定, .ps1 保留作 reference

== 测试 / 验证 ==

- 手动跑 `scripts\run_daily_report.cmd --skip-fetch --skip-md --skip-html --skip-dashboard`
  - exit 0, log 写到 output\logs\cron_2026-07-27.log (3.3KB)
- 跑 `scripts\install_task.cmd` 2 次, task 仍 1 个, Next Run Time 更新正常 (幂等)
- 39/39 tests pass (test_version_match regex 加 letter suffix 支持)
- P7-5 baseline 重抓 (4 天市场自然 drift, residuals 变化大)
  - 旧 baseline (v0.6.8f, 2026-07-23) → 新 baseline (覆盖 v068f.json, 2026-07-27)

== 设计决策 (写进 user memory / future engineering) ==

- "ASCII-only in .cmd/.ps1" (新, 跨项目): 含中文的 .cmd 在 PowerShell `&` 调用时被 preprocess 成乱码
  (PowerShell 把 .cmd 内容当 text 解析, 中文 byte 序列当命令名). 跟 setup_vault.ps1 经验一致
- "admin cmd 跑 schtasks" (新): `Register-ScheduledTask` (PS) 在 non-elevated session 报 "Access is denied",
  `schtasks /Create` (cmd) 在 admin cmd 下稳定. 优先 .cmd, .ps1 保留作备选
- "PROJECT_ROOT 解析 idiom" (新, 跨 .cmd): `set P=%~dp0` + strip trailing `\` + `\..` + `for %%I in ("%P%") do set P=%%~fI`
  解决 `for %%I in ("%~dp0.")` 不解析 `..` 的坑
- "log 路径用 PowerShell Get-Date" (新): `wmic os get localdate` + `%VAR:~0,10%` 在 PS 5.1 + GBK 环境
  出乱码 (变成 `cron_~0,10.log`), `for /f ... 'powershell ... (Get-Date ...)'` 更稳
- "exit code passthrough" (新): .cmd 结尾 `exit /b %ERRORLEVEL%` 让 P8 alert check 能 catch cron 失败
- "P7-5 baseline 季度重算" (已写, 现在就执行): 4 天市场 drift, baseline 必须跟实际同步

== 已知限制 (v0.6.8m) ==

- task 没设 Start In (working dir): schtasks /Create 不支持, .cmd 内部 pushd 处理
- 没 timezone-aware date: log 日期用 Asia/Shanghai (machine local), 美股夏令时后可能差 1 天
- 没脱机 catchup: Start-WhenAvailable 是 task setting, 实际看 Windows 版本

== 文件清单 (v0.6.8m) ==

- scripts/run_daily_report.cmd (1.5KB, 新)
- scripts/install_task.cmd (1.9KB, 新)
- scripts/setup_windows_task.ps1 (4.1KB, 新, 备选)
- docs/PHASE5.md (P5-3/P5-4 改 ✅ done)
- CHANGELOG.md (v0.6.8m 段)
- README.md (5 分钟配 cron 段, 路线图标 done)
- VERSION (0.6.8 → 0.6.8m)
- data/baseline/residuals_v068f.json (覆盖, 4 天后重抓)
- tests/test_smoke.py (test_version_match regex 加 letter suffix)
